# AGENTS.md

本文档是本仓库中所有开发者与 AI Agent 的长期协作约定。除非用户在具体任务中明确覆盖，本文件适用于整个仓库。

## 1. 项目愿景

本项目名称为 **SDK Version Manager（SVM）**，命令行程序固定使用 `svm`，目标是打造一个跨平台、本地优先的开发环境管理工具。

它不只管理 Flutter，而是通过统一模型管理：

- SDK / Runtime：Flutter、Dart、Java、Python、Node.js、PHP 等；
- Web 服务与站点：Nginx、Apache，以及站点、域名、端口、HTTPS 和配置；
- 数据库：MySQL、PostgreSQL、MongoDB 等；
- 基础服务：Redis，以及后续可扩展的本地开发服务；
- 项目环境：按项目选择 SDK 版本、环境变量和依赖服务；
- 本地开发栈：以可复用的环境配置组合多个 Runtime、站点和服务。

产品必须同时提供 GUI 和 CLI。GUI 用于发现、下载和管理指定 SDK 版本；CLI 用于在终端和当前项目目录中初始化、选择、激活与检查环境。两者必须复用同一核心层、Provider、配置格式和状态存储，不能形成两套行为不一致的实现。

## 2. 当前技术基线

- 桌面框架：Qt 6；
- 核心语言：现代 C++；
- 界面：QML / Qt Quick Controls；
- 构建系统：CMake；
- 当前应用目标：`appsdk_version_manager`；
- 最低原则：核心层与界面层分离，平台差异集中封装。

新增代码应遵循项目实际支持的 Qt 与编译器版本。引入新的第三方依赖前，必须说明用途、体积、许可证、跨平台影响和不可替代性。

## 3. 开发目标

### 阶段 0：工程基础

- 明确领域模型、模块边界和目录结构；
- 建立统一错误、日志、任务进度和取消机制；
- 建立自动化测试、代码格式化和基础 CI；
- 实现系统能力探测、命令执行和安全的文件操作抽象；
- 形成 Provider 插件接口，避免为每种工具复制整套业务逻辑。

### 阶段 1：SDK 管理 MVP

首个可用版本优先支持 Windows，并保持核心接口可移植到 macOS 与 Linux。

- 首批 Provider：Flutter、Java、Python、Node.js；
- 检测系统中已安装的版本；
- 获取可安装版本列表；
- 在 GUI 中搜索、选择并下载指定 SDK 版本；
- 下载、校验、安装、卸载和修复版本；
- 设置全局默认版本；
- 通过 CLI 在当前目录初始化项目环境；
- 通过 CLI 为当前项目选择、固定和激活 SDK 版本；
- 展示安装目录、来源、架构、状态和磁盘占用；
- 安全生成或更新 PATH 等环境配置；
- 下载与安装任务支持进度、取消、失败恢复和明确错误信息。

首期 CLI 交互以以下形式为目标：

```text
svm init
svm use flutter 3.38.0
svm use node 24.0.0
svm active
svm list
svm install flutter 3.38.0
svm doctor
svm java --version
svm flutter doctor
svm node app.js
```

- `init`：在当前目录创建项目环境声明，不覆盖已有有效配置；
- `use <provider> <version>`：确认版本可用后，将其绑定到当前项目；是否自动安装由显式参数或交互确认决定；
- `active`：显示当前目录最终生效的 SDK、版本、来源与解析路径；
- `list`：列出本机已安装版本，可按 Provider 筛选；
- `install`：下载、校验并安装指定版本；
- `doctor`：检查配置、路径、Provider、权限和受管理安装的健康状态。
- `svm <provider> [arguments...]`：解析当前目录最终生效的 SDK，并执行该 SDK 的原生命令。例如 `svm java --version` 必须执行当前项目绑定的 Java SDK 中的 `java --version`，不能直接调用系统 PATH 中碰巧存在的 Java。

可以为兼容用户习惯提供 `activate`、`current` 等别名，但帮助文档和输出必须明确唯一的推荐命令。非交互环境中不得等待确认，应通过退出码和错误输出说明所需操作。

SDK 命令代理必须遵守以下契约：

- 从当前工作目录向父目录查找最近的 SVM 项目环境声明；
- 使用项目绑定的版本；项目没有绑定时，才按照明确且可配置的规则回退到全局默认版本；
- 从受管理版本的安装记录解析真实可执行文件，不依赖修改后的系统 PATH 来“碰运气”；
- 将 Provider 名后的参数逐项原样传递给目标程序，不经 shell 拼接或二次解析；
- 默认继承当前工作目录、标准输入、标准输出和标准错误，使交互式命令可正常工作；
- 透传目标进程的退出码，并正确转发可支持的中断信号；
- 为子进程构造仅对本次执行生效的环境变量和 PATH，不永久修改系统环境；
- 找不到项目配置、绑定版本、安装记录或目标可执行文件时，给出明确诊断和修复命令；
- `svm java --version`、`svm node app.js` 等代理命令不得被同名的 SVM 内置命令歧义覆盖；
- Provider 可以声明多个入口，例如 Java 的 `java`、`javac`、`jar`，后续通过清晰语法暴露，不能硬编码在 CLI 主程序中。

### 阶段 2：Web 站点管理

- 管理 Nginx，随后扩展 Apache；
- 创建、启用、停用、复制和删除站点；
- 配置站点根目录、域名、端口、反向代理和 Runtime；
- 校验配置后再应用，失败时保留或恢复上一份有效配置；
- 支持本地 HTTPS 证书接入，但不隐式修改系统信任；
- 检测端口冲突、配置冲突和权限问题；
- 保留“查看生成配置”和“打开配置目录”的能力。

### 阶段 3：数据库与基础服务

- 首批支持 MySQL、PostgreSQL、MongoDB、Redis；
- 支持版本安装、实例创建、启动、停止、重启和状态检查；
- 每个实例拥有独立的数据目录、配置、端口和日志；
- 卸载程序与删除用户数据必须是两个独立操作；
- 删除数据、重置密码、覆盖配置等高风险操作必须二次确认；
- 数据库备份与恢复能力成熟前，不宣称数据安全保障。

### 阶段 4：开发栈与生态

- 用声明式配置描述项目所需 SDK、站点、数据库和服务；
- 支持环境导入、导出、复制和差异检查；
- 扩展 CLI 的脚本化、补全和自动化能力；
- 完善 macOS 与 Linux 支持；
- 建立稳定的 Provider 扩展机制和开发文档；
- 在安全边界清晰后再评估远程源、插件市场和团队共享能力。

## 4. 产品范围与非目标

首期不是 Docker、虚拟机或完整容器编排工具，也不以替代系统包管理器为目标。

以下事项不应在 MVP 中优先实现：

- 云端账号、团队协作或远程部署；
- 生产服务器运维；
- 任意脚本市场；
- 静默获取管理员权限；
- 在缺乏回滚能力时自动改写大量系统配置；
- 同时支持所有语言、数据库和 Web 服务器。

新增 Provider 应在通用接口稳定后逐个接入。优先保证少量 Provider 的完整体验，而不是大量仅能安装的 Provider。

## 5. 核心领域模型

实现时优先围绕以下概念建模，避免界面直接拼接命令：

- `Provider`：某类 SDK 或服务的能力实现；
- `Package`：可下载、安装和校验的软件发行物；
- `InstalledVersion`：本机已安装的具体版本；
- `VersionSource`：官方或用户配置的版本与下载源；
- `ProjectBinding`：项目目录与 SDK 版本、环境配置的绑定；
- `ServiceDefinition`：Nginx、数据库、Redis 等服务类型定义；
- `ServiceInstance`：具有独立配置、端口、目录和生命周期的实例；
- `Site`：域名、根目录、端口、代理和 Runtime 的组合；
- `Environment`：一组可复现的 SDK、站点和服务声明；
- `Operation`：可观察、可取消、可记录结果的长任务；
- `PlatformAdapter`：进程、权限、环境变量、服务管理和文件系统的平台实现。

Provider 能力应显式声明，例如发现版本、安装、卸载、激活、启动服务或健康检查。不要假定所有 Provider 都支持相同操作。

## 6. 建议架构

依赖方向应保持单向：

```text
QML UI ──→ GUI Application / ViewModel ──┐
                                         ├──→ Domain
CLI ─────→ CLI Application / Presenter ──┘       ↓
                                           Provider Interfaces
                                                  ↓
                                  Provider + Platform Implementations
```

约束如下：

- QML 只负责展示、输入和交互状态，不执行 shell 命令；
- CLI 只负责参数解析、终端交互和输出格式，不复制业务逻辑；
- SDK 代理执行必须经过统一的版本解析器和进程执行器；
- Domain 层不依赖 QML，也不包含平台专属路径；
- Provider 不直接弹窗；
- 所有外部进程调用经过统一执行器；
- 网络下载、解压、校验、安装与激活是可区分的步骤；
- 平台代码放入明确的平台实现中，禁止散落条件编译；
- 配置格式必须带 schema 版本，并为迁移预留机制；
- GUI 发起的安装任务与 CLI 发起的安装任务具有一致的锁、进度、取消和错误语义；
- 密码、令牌等秘密不得写入普通日志或提交到仓库。

建议目录随首轮重构逐步形成：

```text
src/
  app/
  domain/
  infrastructure/
  platforms/
  providers/
  ui/
tests/
  unit/
  integration/
docs/
```

不要仅为匹配目录建议而进行无价值搬迁；创建新模块时再逐步落地。

## 7. 安全与数据保护

本项目会下载可执行文件、修改环境变量、管理服务并接触数据库数据，安全优先级高于便利性。

- 默认使用普通用户权限，仅在必要步骤请求提权；
- 提权前说明具体动作与影响范围；
- 禁止通过字符串拼接构造 shell 命令；
- 参数应以结构化列表传递给进程执行器；
- 下载文件应优先校验官方哈希或签名；
- TLS 校验不得默认关闭；
- 压缩包解压必须防止路径穿越和符号链接逃逸；
- 删除前解析并校验规范路径，禁止删除工作区、用户目录根或磁盘根；
- 默认只删除由本应用创建且有记录的安装目录；
- 用户数据删除与软件卸载分离，并提供清晰确认；
- 修改配置采用“生成临时文件、校验、原子替换”的流程；
- 可恢复操作优先于永久删除；
- 日志应自动脱敏，不记录密码、令牌、连接串中的秘密部分；
- 不执行来源不明的安装脚本，不把远程文本直接交给 shell。

## 8. Provider 设计要求

每个 Provider 至少应说明：

- 唯一 ID、显示名称、类别和支持平台；
- 版本发现来源及其可信边界；
- 支持的 CPU 架构；
- 安装布局和所有权标记；
- 版本检测、健康检查和可用性判断方式；
- 支持的操作与不支持的操作；
- 下载校验方式；
- 激活方式以及对 PATH、链接或包装器的影响；
- 卸载行为是否涉及用户数据；
- 所需权限；
- 单元测试与至少一个受控集成测试方案。

版本比较不得直接按普通字符串排序。Provider 应将原始版本保留下来，并提供规范化版本或自身比较策略。

## 9. 状态、配置与可复现性

- 应用自身状态与受管理软件的文件分开存放；
- 安装记录不能作为唯一事实来源，必须能重新扫描并修复状态；
- 用户手动安装的软件默认视为外部资源，未接管前不得删除；
- 配置写入必须可检测冲突，不能悄悄覆盖用户在应用外的修改；
- 项目级声明优先使用可提交、可读、可比较的文本格式；
- CLI 从当前目录向父目录查找最近的项目环境声明，并明确显示实际命中的配置文件；
- 项目环境声明至少记录 schema 版本和 `provider -> version` 绑定，不写入可从本机状态推导出的绝对安装路径；
- 同一项目可同时绑定多个 Provider，例如 Flutter、Java 和 Node.js；
- 项目绑定优先于全局默认值；未绑定时是否回退到全局版本必须可预测并可由 `active` 查看；
- 项目初始化、绑定修改和迁移应采用原子写入，避免中断后留下半份配置；
- 本机路径、密钥和机器专属状态不得无意写入项目声明；
- 任何缓存都应可重建，删除缓存不应破坏已安装环境。
- 所有 Provider 的版本索引采用相同的缓存优先策略：普通进入先读本地缓存，仅在缓存不存在时请求官方源；
- 用户主动点击刷新时才强制重新请求版本索引，并在成功解析后原子替换对应 Provider 的缓存；
- Provider 目录不得内置会短暂显示的演示版本；请求期间应显示加载状态，失败时保留上一份有效缓存。

## 10. UI / UX 原则

- 首页优先回答：装了什么、正在运行什么、哪里有问题；
- 所有耗时操作展示阶段、进度、速度或可解释的等待状态；
- SDK 页面支持按 Provider、版本、稳定通道和安装状态筛选，并可直接选择版本下载；
- GUI 必须清楚区分“已下载”“已安装”“项目正在使用”和“全局默认”；
- 用户可以取消安全可取消的操作；
- 错误信息包含失败动作、可能原因和可执行的恢复建议；
- 危险按钮与普通操作在视觉和文案上明确区分；
- 不用“成功”掩盖部分失败；
- 支持键盘操作、合理焦点顺序和基础无障碍语义；
- 界面文案支持国际化，禁止在业务代码中散落用户可见字符串；
- 深色与浅色主题均需可读；
- UI 不应冻结，网络、进程和磁盘重任务不得阻塞主线程。

## 11. 编码规范

- 使用现代 C++ 的 RAII、强类型和明确所有权；
- 优先值语义和 Qt/C++ 标准设施，避免不必要的全局单例；
- QObject 跨线程使用必须明确线程归属；
- 不捕获生命周期不明的裸指针执行异步回调；
- 公共接口使用清楚的结果类型表达成功、失败和取消；
- 不用布尔值同时表达多个状态；
- 用户可见错误与诊断详情分离；
- 新功能不得只在 QML 中实现业务规则；
- CLI 的普通输出写入标准输出，诊断和错误写入标准错误，并使用稳定、可测试的退出码；
- CLI 命令应支持 `--help`；面向脚本的命令逐步提供结构化输出选项；
- 注释解释“为什么”，不重复代码表面行为；
- 保持改动聚焦，不顺手重写无关模块；
- 不提交构建产物、IDE 用户配置、缓存、下载包或秘密。

命名遵循项目既有风格；在形成统一格式化配置前，新增 C++ 代码采用 Qt 常见风格，并保持同一文件内一致。

## 12. 测试与完成标准

每项功能按风险选择测试层级：

- Domain 和版本解析：快速单元测试；
- Provider：使用固定样本、伪下载源或受控目录测试；
- 文件、进程和配置操作：集成测试；
- 关键用户路径：最小 UI 冒烟测试；
- CLI：参数解析、目录向上查找、配置优先级、退出码和非交互行为测试；
- SDK 命令代理：参数透明传递、工作目录、临时环境、标准流、退出码和中断处理测试；
- 平台专属行为：对应平台测试，不能用另一平台的通过结果代替。

涉及安装、卸载、配置替换或数据删除时，必须测试失败中断、重复执行和部分完成状态。

任务完成至少满足：

- 项目可配置并成功编译；
- 相关测试通过；
- 新增错误路径有可理解的反馈；
- 没有把秘密、机器专属路径或构建产物加入版本控制；
- 用户数据和外部安装不会被误删；
- 新增行为有必要的文档；
- 已知限制在交付说明中明确列出。

如果当前环境无法执行某项验证，应说明未验证内容与原因，不得声称已经通过。

## 13. Agent 工作流程

处理代码任务时：

1. 先阅读本文件、相关代码、构建配置和测试；
2. 明确任务属于哪个阶段及模块，避免扩大范围；
3. 优先修复根因，并维持已有行为兼容；
4. 实现最小而完整的纵向切片；
5. 运行与风险相称的构建和测试；
6. 检查变更，不覆盖用户的无关修改；
7. 汇报实现结果、验证方式、已知限制和后续建议。

遇到以下情况应暂停并向用户确认：

- 需要永久删除用户数据库或站点数据；
- 需要更改产品核心范围或技术栈；
- 需要引入体积较大或许可证存疑的依赖；
- 需要修改系统级配置且无法可靠回滚；
- 不同方案会造成明显不兼容，而需求没有给出取舍。

## 14. 近期优先事项

在开始大规模界面开发前，优先完成：

1. 确定应用 ID 和首发平台，CLI 名称固定为 `svm`；
2. 建立 `src`、`tests` 与基础 CI；
3. 设计 Provider、Operation、InstalledVersion 和 PlatformAdapter 接口；
4. 实现只读的本机 SDK 扫描；
5. 确定项目环境声明格式，实现 `init`、`use` 和 `active`；
6. 实现 `svm <provider> [arguments...]` 的当前项目 SDK 命令代理；
7. 用一个 Provider 打通版本列表、GUI/CLI 下载、校验、安装、激活、代理执行和卸载；
8. 再把相同流程验证到第二个差异较大的 Provider；
9. 接入 GUI 的版本选择、任务进度、错误和取消状态；
10. 稳定 SDK MVP 后再进入站点与数据库管理。

建议第一个完整 Provider 选择 Flutter 或 Node.js；第二个选择 Java 或 Python，以验证压缩包布局、版本规则和激活机制的差异。
